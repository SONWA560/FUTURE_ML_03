name: Keep backend warm
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sSf "${{ secrets.BACKEND_URL }}/api/job-roles" > /dev/null
